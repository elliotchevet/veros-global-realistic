[metadata]
description_file = README.md


[flake8]
exclude = __init__.py
ignore =
    # Comments
    E26,
    # Lines starting with binary operators
    W503,
max-line-length = 120

[tool:pytest]
testpaths = test
addopts = --forked
